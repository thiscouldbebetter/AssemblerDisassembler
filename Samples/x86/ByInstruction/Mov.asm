mov ax, ax

mov ax, bx
mov bx, ax

mov ax, cx
mov cx, ax

mov ax, dx
mov dx, ax

mov bx, bx
mov cx, cx
mov dx, dx

mov ax, [bx]
mov [bx], ax

mov ax, [bx+1]
mov ax, [bx+2]
mov [bx+1], ax
